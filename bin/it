#!/usr/bin/env php
<?php

declare(strict_types=1);

require $_composer_autoload_path ?? __DIR__ . '/../vendor/autoload.php';

use IT\Command\CheckCommand;

$command = new CheckCommand();
exit($command->run($argv));
